import { Typography, Box } from "@mui/material";

const Profile = () => {
  return (
    <Box>
      <Typography variant="h4">My Profile</Typography>
      <Typography>Your profile details will be shown here.</Typography>
    </Box>
  );
};

export default Profile;
