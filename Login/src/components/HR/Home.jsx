import { Typography } from "@mui/material";

const Home = () => {
  return (
    <div>
      <Typography variant="h4">Welcome, HR!</Typography>
      <p>Manage employees and verify documents easily.</p>
    </div>
  );
};

export default Home;
